server:
  port: 8765

eureka:
  client:
    fetchRegistry: true

zuul:
  routes:
    bugalu-twitter-streaming-service:
      path: /api/**
      serviceId: bugalu-twitter-streaming-service
    bugalu-nlp-analyzer-service:
      path: /**
      serviceId: bugalu-nlp-analyzer-service
